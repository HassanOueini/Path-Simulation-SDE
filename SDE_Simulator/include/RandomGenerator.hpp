#ifndef RANDOMGENERATOR_HPP
#define RANDOMGENERATOR_HPP

class RandomGenerator {
public:
    RandomGenerator();
    double generateGaussian(); // Returns a random number from a normal distribution
};

#endif
